# Apps Script Web App endpoint for enrollment form.
# 取得方式看 gas/README.md。
# 本機開發時複製成 .env.local（已在 .gitignore 內，不會進 git）。
NEXT_PUBLIC_ENROLL_ENDPOINT=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
